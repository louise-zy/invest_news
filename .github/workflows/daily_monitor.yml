name: ESDM Monitor Daily

on:
  schedule:
    # Runs at Beijing Time 09:30 (01:30 UTC) and 13:30 (05:30 UTC)
    - cron: '30 1,5 * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  run-monitor:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.9'
        
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        if [ -f requirements.txt ]; then pip install -r requirements.txt; fi
        
    - name: Run Monitor Script
      env:
        DINGTALK_WEBHOOK: ${{ secrets.DINGTALK_WEBHOOK }}
        DINGTALK_SECRET: ${{ secrets.DINGTALK_SECRET }}
        LLM_API_KEY: ${{ secrets.LLM_API_KEY }}
      run: |
        python esdm_monitor.py

    - name: Commit Database Changes
      run: |
        git config --global user.name 'github-actions[bot]'
        git config --global user.email 'github-actions[bot]@users.noreply.github.com'
        git add esdm_news.db
        git diff --quiet && git diff --staged --quiet || (git commit -m "update: esdm_news.db processed records" && git push)
